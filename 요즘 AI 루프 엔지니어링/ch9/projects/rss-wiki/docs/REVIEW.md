# REVIEW: T11b

- 평가 시각: 2026-09-18T05:40:10+09:00
- 대상: docs/IMPL.md에 적힌 처리 항목 **T11b 하나**
  (`cli.py` `_run` 전체 구현 + `--config`/`--max-summaries`, `tests/test_cli.py` 19경우,
  `tests/test_summarize_pending.py` 두 줄 수정, `tests/test_config.py` 1건, README/`pyproject.toml`, 뮤테이션 10건)
- 화면 축 적용 여부: **적용하지 않는다.** 이 항목의 산출물은 터미널 stdout 여섯 줄과
  `--help` 텍스트뿐이고, 평가 지침이 화면 축에서 보라고 한 여백/타이포그래피/색이
  존재하지 않는다. 따라서 4축 12점 만점, 9점 이상 PASS 기준을 적용한다.
  (대신 "주요 동작의 발견 가능성"에 해당하는 CLI 관찰은 운영 고려 축에서 봤다.)

## 평가 표

| 축 | 점수 | 근거 |
| --- | --- | --- |
| 사양 충족 | 3 | acceptance 13항목을 전부 직접 재현했다. (1) `uv run pytest tests/test_cli.py -v` → **19 passed**. TASKS (a)~(n) 14경우가 각각 별도 테스트로 수집되고((j)는 파라미터화 3경우 `[0-0-False]`/`[1-0-True]`/`[0-1-True]`), 기존 3개(`test_entry_point_help_exits_zero_and_lists_run` `tests/test_cli.py:17`, `test_no_subcommand_returns_one` `:27`, `test_unknown_subcommand_exits_with_code_two` `:31`)가 이름 그대로 남아 통과한다(3 + 13 + 3 = 19로 산술이 맞는다). (2) `uv run pytest tests/test_summarize_pending.py -v` → **11 passed**(IMPL이 적은 작업 전/후 동일값과 일치). (3) `uv run pytest tests/test_config.py -v` → **24 passed**, `test_load_config_null_url_raises`(`tests/test_config.py:63`)가 기존 `missing_url` 테스트와 별도로 존재. (4) `uv run pytest -q` → **367 passed**, `TZ=UTC uv run pytest -q` → **367 passed**(기준선 351 + 16). (5) `uv run rss-wiki run --help` → 종료 코드 0, 출력에 `--config CONFIG`와 `--max-summaries MAX_SUMMARIES` 모두 포함. (6) `uv run rss-wiki run --config /tmp/rss-wiki-nope.yaml` → 종료 코드 **1**, uv 경고를 뺀 stderr가 정확히 **한 줄** `오류: 설정 파일을 찾을 수 없습니다: /tmp/rss-wiki-nope.yaml`, 스택 트레이스 없음. (7) 시나리오 검증 — 빈 임시 디렉터리 `/tmp/rsswiki-eval`에 `feeds: []`만 적고 **서브프로세스로** `uv run rss-wiki run --config …` 실행 → 종료 코드 0, 출력이 여섯 줄 전부 `: 0`, `ls -R`로 `data/rss-wiki.db`와 `wiki/index.md` 실제 생성 확인. (8)~(12) grep 4건이 모두 결과 없음(`not implemented` / `except Exception\|except BaseException` / `Add your description here` / `import (httpx\|trafilatura\|feedparser\|sqlite3)`)이고 README grep은 결과 있음(`:14` `cp feeds.example.yaml`, `:24`/`:26` `claude`, `:35`/`:40` `--max-summaries`). (13) shasum 8줄 일치(아래 모듈 경계). 구현도 TASKS 문구와 줄 단위로 대조했다 — `src/rss_wiki/cli.py:4-7` 이름 import 4줄, `:9-10` 상수 2개, `:14-18` `ConfigError`, `:20-24` `SchemaVersionError`, `:26-35` `try/finally conn.close()`, `:27` `collect`, `:29-32` `summarize_pending` + `ClaudeUnavailableError`(이 경로에서 `write_wiki` 미호출), `:33` `write_wiki`, `:37-42` 여섯 줄 리터럴과 순서, `:43-44` 조건부 안내 줄, `:57-58` 두 인자, `:72-75` `func` 누락 방어. PRD 4.6이 요구한 "전체 실행 + 새 글/성공/실패/포기 출력"이 실제 실행으로 닫혔다. |
| 모듈 경계 | 3 | 변경 파일이 touch 범위 6개(`src/rss_wiki/cli.py`, `tests/test_cli.py`, `tests/test_summarize_pending.py`, `tests/test_config.py`, `README.md`, `pyproject.toml`)와 정확히 일치한다. untracked라 `git diff`를 못 쓰므로 두 경로로 확인했다 — (가) 손대지 않기로 한 src 8개의 `shasum`을 평가 시점에 직접 재서 IMPL 블록과 **8줄 모두 일치**(`config.py 796d853f…`, `db.py 569576cc…`, `extract.py f1f1c232…`, `fetch.py 702c7541…`, `pipeline.py 0bed1122…`, `summarize.py e4283936…`, `timeutil.py 25c68bec…`, `wiki.py 3480e4ea…`). (나) `ls -lT`로 mtime을 정렬하니 이번 사이클 흔적이 `cli.py 05:31`과 나머지 다섯 개 `05:33`뿐이고, 다른 src/tests는 전부 `≤05:16`(직전 T11a의 `db.py`/`pipeline.py` 05:15, `test_db.py` 05:16)에서 멈춰 있다. 범위 밖 변경이 없다. 책임 분리도 지켜졌다 — `cli.py`에 파이프라인 로직이 들어가지 않았고(집계는 `collected`/`summarized` 필드를 읽기만 한다), `httpx`/`sqlite3`/`feedparser`/`trafilatura`를 import 하지 않으며(grep 결과 없음), 파이프라인 함수를 모듈 전역 이름으로 참조해 테스트가 `monkeypatch.setattr(cli, …)`로 갈아끼울 수 있다(`tests/test_cli.py:76-80`). 기존 함수 시그니처 변경 없이 있는 것만 호출한다. |
| 테스트 충실도 | 3 | TASKS 지정 뮤테이션 **10건을 임시 복사본(`/tmp/rsswiki-mut`, `PYTHONPATH=…/src …/.venv/bin/python -c "import rss_wiki.cli as m; print(m.__file__)"`로 복사본 로드를 먼저 확인, 매회 원본 복사로 복구)에서 전부 독립 재현했고 10건 모두 사망**했다. 재현한 마지막 요약 줄이 IMPL 기록과 **한 글자도 다르지 않다** — (1) `1 failed, 366 passed` (2) `1 failed, 366 passed` (3) `1 failed, 366 passed` (4) `1 failed, 366 passed` (5) `2 failed, 365 passed` (6) `1 failed, 18 passed, 348 deselected` (7) `3 failed, 16 passed, 348 deselected` (8) `3 failed, 16 passed, 348 deselected` (9) `1 failed, 18 passed, 348 deselected` (10) `1 failed, 10 passed, 356 deselected`. 직전 REVIEW 메모 2(뮤테이션 `N passed`를 실제 출력에서 그대로 옮길 것)가 이번엔 완전히 지켜졌다. 특히 **(10)은 T11a에서 `35 passed`로 생존했던 바로 그 뮤테이션**(`src/rss_wiki/pipeline.py:202` 뒤에 같은 `list_articles` 호출 한 줄 추가)이고, `tests/test_summarize_pending.py:110-112`의 `len(list_calls) == 1` 단언으로 사망한다 — T11a REVIEW 메모 1이 닫혔음이 실측으로 확인됐다. 테스트가 요구를 실제로 검증하는지도 봤다 — 실패 경로 4종이 모두 별도 테스트다(`ConfigError` `:128`에서 `calls == []`로 후속 미호출까지, `SchemaVersionError` `:155`에서 `collect` 미호출까지, `ClaudeUnavailableError` `:181`에서 `calls == [("close",)]`로 `write_wiki` 미호출 **와** conn 종료를 동시에, `sqlite3.OperationalError` `:249`에서 `pytest.raises`로 전파 **와** conn 종료를 동시에). 인자/순서는 동일성(`is`)으로 고정했고(`:229-237` conn/`config.feeds`/`config.wiki_dir`), 성공 경로 출력은 `lines == [...]` 리스트 전체 비교라 순서와 줄 모양이 함께 잠긴다(`:94-101`). (n) 통합 테스트(`:338`)는 주입 없이 실제 `load_config`/`connect`/`collect`/`summarize_pending`/`write_wiki`를 돌려 `wiki/index.md` 생성까지 보는데, 피드 0개라 `check_claude`가 불리지 않아 `claude` 설치 여부와 무관하게 통과한다 — 평가자 환경에서도 그대로 통과했다. 기대 문구와 기본값은 전부 테스트 안 리터럴이고 `cli.DEFAULT_*`를 되먹이지 않는다(`:111` `"feeds.yaml"`, `:113` `20`) — 직전 REVIEW 메모 3과 T6e 메모 1의 방향을 이번엔 지켰다. 남은 틈은 아래 메모 1(경미)뿐이라 감점하지 않는다. |
| 운영 고려 | 3 | 새 의존성 없음(`pyproject.toml`은 `description` 한 줄만 교체, `:4`가 README 첫 줄 문장과 정확히 같다). 문서가 실제 동작과 맞물린다 — `cli.py:44`가 출력하는 안내 줄의 `README의 "포기한 글/피드 다시 시도하기"`가 `README.md:53`의 실제 절 제목과 글자까지 일치해, 사용자가 안내를 따라갔을 때 빈 참조가 되지 않는다(이전 사이클들이 T11로 미뤄 둔 T8b 메모 5 / T7b 메모 5 / T2 메모 3이 여기서 한꺼번에 닫혔다). README "설정"(`:11-20`)은 `cp feeds.example.yaml feeds.yaml` 준비 단계와 `wiki_dir`/`db_path` 기본값, 설정 오류 시 한 줄 + 종료 코드 1을 적었고, "전제"(`:22-27`)는 요약할 글이 **있을 때만** 사전 확인 1회가 프롬프트를 소모한다는 T11a 자체 결정까지 옮겼다. "실행"(`:35-45`)은 두 인자와 여섯 줄의 의미를 적었다. IMPL의 프로세스 요구도 지켜졌다 — 미착수 항목 없음 명시, 작업 전 shasum을 첫 편집 전에 쟀다는 기록, 복사본 로드 확인 절차, 뮤테이션 후 복구와 임시 디렉터리 삭제(평가 시점 `/tmp` 잔여 없음). IMPL의 모든 개수를 평가자가 재현해 **불일치가 한 건도 없었다**(367 / 19 / 11 / 24 / 뮤테이션 10줄). 남은 흠은 아래 메모 2(`--help`의 인자 설명 부재)인데, 축 기준이 묻는 "의존성과 문서"는 채워져 있어 감점하지 않고 메모로 넘긴다. |

**합계 12 / 12 → PASS** (화면 없는 항목 기준 9점 이상)

## 합격 여부

**PASS.** T11b가 닫히면서 TASKS의 마지막 미완료 항목이 사라졌고, PRD 4.6(`rss-wiki run` 전체 실행과 결과 요약 출력)과 PRD 4.4의 실행당 상한이 사용자에게 실제로 닿는 경로를 갖췄다. PRD 7절의 "`claude` 없음/미로그인은 명확한 오류로 종료"도 종료 코드 1로 연결됐다. 조건부가 아닌 완전 PASS이므로 다음 사이클로 강제되는 필수 메모는 없다.

## decisions 처리

`docs/decisions/`의 11건(`article-filename` / `claude-options` / `content-length` / `first-run-limit` / `first-run-skipped-keys` / `give-up-threshold` / `index-page` / `paths` / `per-run-summary-limit` / `tag-normalization` / `timestamp-format`)은 모두 `합의: …`와 `반영 완료: PRD …` 두 줄을 가진 **닫힌 결정**이고 `.planner.md`/`.evaluator.md` 짝이 애초에 존재하지 않는다. 평가자 선택을 새로 기록해야 하는 열린 주제가 없어 evaluator 파일을 만들지 않았다(직전 사이클과 같은 판단).

## 다음 사이클로 넘기는 메모

1. (정보, 보류 권고) **stderr의 `오류: ` 접두사는 어느 테스트도 고정하지 않는다.** 세 오류 테스트(`tests/test_cli.py:151`, `:177`, `:212`)가 예외 메시지 부분 문자열만 보므로, `cli.py:17`/`:23`/`:31`의 `f"오류: {exc}"`에서 접두사를 떼도 19개가 전부 통과한다. 다만 접두사는 사람이 읽는 장식이고 실제 실행(`오류: 설정 파일을 찾을 수 없습니다: …`)으로 확인되므로, **새 항목을 만들지 말 것을 권고한다.** 나중에 오류 출력 형식을 바꾸는 항목이 생기면 그때 한 줄로 함께 고정하면 된다.
2. (정보) **`run --help`가 두 인자의 뜻을 설명하지 않는다.** `run_parser.add_argument`(`cli.py:57-58`)에 `help=`가 없어 `--config CONFIG` / `--max-summaries MAX_SUMMARIES`라는 메타변수만 나온다. 의미는 README에만 있다. TASKS가 요구한 적이 없고 동작에도 영향이 없으니 이번 PASS를 막지 않지만, 다음에 `cli.py`를 만지는 일이 생기면 `help=` 두 줄이 가장 값싼 개선이다.
3. (정보) **`--max-summaries`에 음수/0을 주면 조용히 아무 요약도 하지 않고 종료 코드 0으로 끝난다.** `summarize_pending`의 입구 가드(T11a 자체 결정, `pipeline.py:199-200`)가 SQLite `LIMIT -1` 문제를 정확히 막고 있어 **버그가 아니다**. 다만 CLI 층에는 `type=int`만 있어 사용자가 오타로 `-1`을 넣으면 "요약 성공: 0"만 보게 된다. 항목화할 만큼은 아니라고 본다.
4. (사람 검토, **16사이클째**) `src/rss_wiki/` 전체와 `tests/`, `docs/`가 여전히 untracked다. 이번 사이클은 shasum 8줄 일치 + `ls -lT` mtime 두 경로로 범위를 검증했고 실제로 깨끗했지만, 이건 매 사이클 평가자가 우회로를 새로 만들어 내는 비용이다. **이제 TASKS가 비었으므로 커밋하기에 가장 좋은 시점이다** — 한 번 커밋해 두면 다음부터 `git diff --stat` 한 줄로 끝난다. Planner는 Bash가 없고 Evaluator는 `src/`/`tests/` 수정 권한이 없어 사람이 해야 한다.
5. (정보, 다음 planner용) **TASKS의 미완료 항목이 0개가 됐다.** M1~M5가 전부 `[x]`이고 PRD 4.1~4.6 / 5절 / 7절에 대응하는 코드가 모두 들어왔다. 다음 사이클은 새 항목을 억지로 만들기보다, (가) 위 메모 4의 커밋, (나) 실제 피드 URL로 한 번 돌려 보는 사람 손 검증(네트워크와 `claude`가 필요해 자동 테스트로는 덮이지 않는 유일한 구간이다), (다) 그 결과로 드러나는 것만 새 항목화 — 이 순서를 권고한다. 현재 367개 테스트 중 실제 HTTP와 `claude` 호출을 하는 것은 하나도 없다는 점이 이 권고의 근거다.
