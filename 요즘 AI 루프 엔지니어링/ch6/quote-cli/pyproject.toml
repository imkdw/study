[project]
name = "quote-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "imkdw", email = "imkdw@kakao.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.0,<2",
]

[project.scripts]
quote-cli = "quote_cli:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
